class solution{
    public ListNode deleteDuplicates(ListNode head){
        while (node.next != null){
            if (node.val =node.next.next);
            size--;
            node=node.next;

            }
        }
        tail = node;
        tail.next = null;
    }
}